{
	"name": "node-postgres-auth",
	"version": "0.0.1",
	"description": "Authentication backend for react-router-auth project",
	"main": "src/server.js",
	"scripts": {
		"build": "rm -rf build && tsc",
		"serve": "rm -rf build && tsc -w",
		"start": "nodemon build/src/server.js"
	},
	"keywords": [
		"node",
		"postgresql",
		"authorisation"
	],
	"author": "Tomasz Chybziński",
	"license": "ISC",
	"dependencies": {
		"body-parser": "^1.19.0",
		"dotenv-defaults": "^1.0.3",
		"express": "^4.17.1",
		"jsonwebtoken": "^8.5.1",
		"pg": "^7.16.1",
		"typescript": "^3.7.4"
	},
	"devDependencies": {
		"@types/express": "^4.17.2",
		"@types/node": "^13.1.1",
		"nodemon": "^2.0.2"
	}
}
